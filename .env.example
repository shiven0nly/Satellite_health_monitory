# Copy to .env and fill in your values before starting the backend.
# The backend will work without GROQ_API_KEY — it just won't produce LLM explanations.

GROQ_API_KEY=your_groq_api_key_here

# Optional: override the receiver URL the backend forwards alerts to
RECEIVER_URL=http://localhost:8502/receive_alert
